dependencies {
    implementation "io.ktor:ktor-client-core:2.0.0"
    implementation "io.ktor:ktor-client-cio:2.0.0"  // Pour le moteur CIO
    implementation "io.ktor:ktor-client-json:2.0.0"  // Sérialisation JSON
    implementation "io.ktor:ktor-client-serialization:2.0.0"  // Sérialisation JSON
}




fun main() = runBlocking {
    // Créer le client Ktor
    val client = HttpClient(CIO) {
        install(ContentNegotiation) {
            json()  // Utilisation de la sérialisation JSON
        }
    }

// Faire la requête GET pour obtenir les films "Now Playing"
        val nowPlayingResponse: NowPlayingResponse = client.get(url)

        // Boucle pour afficher chaque film
        for (movie in nowPlayingResponse.results) {
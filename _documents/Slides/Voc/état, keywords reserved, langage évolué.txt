le langage évolué :
- naturel, performant et technique
- grammaire, manuel, des exemples
- qui cache la complexité technique , du matériel et du système des machines (décoder les mécanismes)

* attributs : relation entre eux c'est un objet (ou une classe). Description de son état.
* identifiant : identifiant métier : il vient du monde réel (géré par le programmeur donc)
* relation : le partage entre des choses dépendantes (table) et indépendantes (testTable) temporaires "tampon"
* état d'un objet :
L’état est une description :
— ce qui le différencie des autres objets du même type
— c’est sa mémoire interne !
— ce que contient un objet à un instant précis : c’est sa mémoire interne
— C’est la combinaison des valeurs de ses attributs à un moment donné : Vehicule{1,"Audi","A3","rouge","150km")
--> une voiture est en mouvement ou à l’arrêt


- mot-clé réservé (pas des identifiants) : if, else, while, function, return, class, include, require, echo, ... (not identifiers, keywords)


On utilise les expressions rationnelles dans le cadre de leur définitions (regular exp)
- sous forme de chaîne de caractères
- pour comparer à des chaînes de caractères
=> En réalité en PHP ce sont des expressions PERL encadrées par des délimiteurs

La structure existe sous forme de caractères spéciaux (Meta) de caractères d'autres caractères
c'est une assersion pas une condition

Une classe de caractères spécifie le caractère du résultat

Les regex en SQL ne sont pas standardisées : leur syntaxe peut légèrement changer d’un SGBD à l’autre

Oui, il est tout à fait possible d’utiliser des expressions régulières (regex) en SQL, mais leur support dépend du SGBD (le système de base de données que tu utilises).
